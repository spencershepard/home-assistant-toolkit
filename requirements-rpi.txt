# Raspberry Pi 3 optimized requirements for STT system
# Minimal set of dependencies for core functionality

# Core audio processing (REQUIRED)
sounddevice==0.5.2
soundfile==0.13.1
# numpy==1.24.4 # Use system numpy or install separately if needed

# Wake word detection (REQUIRED)
pvporcupine==3.0.5

# Speech-to-text (REQUIRED) - Using OpenAI whisper for better RPi compatibility
openai-whisper==20231117

# HTTP requests (REQUIRED)
requests==2.32.5

# Utilities (REQUIRED)
python-dotenv==1.0.1

# Support libraries automatically installed by above packages
# (No need to specify versions - let pip resolve dependencies)
# - ctranslate2 (required by faster-whisper)
# - certifi, urllib3, charset-normalizer, idna (required by requests)
# - cffi, pycparser (required by sounddevice)
# - colorama (cross-platform colored terminal text)
# - packaging (package version handling)

# =====================================
# OPTIONAL FEATURES (uncomment if needed)
# =====================================

# PyTorch-based features (Advanced VAD, etc.)
# WARNING: Large download and compilation time on RPi
# torch==2.1.0+cpu --index-url https://download.pytorch.org/whl/cpu
# torchaudio==2.1.0+cpu --index-url https://download.pytorch.org/whl/cpu

# Speaker identification (Very resource intensive - not recommended for RPi 3)
# resemblyzer==0.1.4
# librosa==0.10.2
# scikit-learn==1.3.2
# scipy==1.11.4

# Additional Porcupine features
# pvrecorder==1.2.7  # Only needed if using pvrecorder instead of sounddevice

# Built-in Python modules (no installation needed):
# platform, os, wave, threading, concurrent.futures, base64, datetime, json